{
  "schema": 1,
  "type": "completion",
  "description": "Generate quiz based on the given user content, number of questions, and type of answers. Respond only with JSON.",
  "execution_settings": {
    "default": {
      "temperature": 0.4,
      "response_format": {
        "type": "json_object"
      }
    }
  },
  "input_variables": [
    {
      "name": "content",
      "description": "Information from attachment provided by user.",
      "required": true
    },
    {
      "name": "numberOfQuestions",
      "description": "Number of questions to generate.",
      "required": true
    },
    {
      "name": "typeOfQuestions",
      "description": "Type of questions to generate.",
      "required": true
    },
    {
      "name": "language",
      "description": "The language in which the quiz should be.",
      "required": true
    }
  ]
}