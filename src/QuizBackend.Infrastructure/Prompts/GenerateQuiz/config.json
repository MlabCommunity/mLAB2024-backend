{
  "schema": 1,
  "type": "completion",
  "description": "Generate quiz based on the given user topic, number of questions, and type of answers. Respond only with JSON.",
  "execution_settings": {
    "default": {
      "temperature": 0.8,
      "response_format": {
        "type": "json_object"
      }
    }
  },
  "input_variables": [
    {
      "name": "content",
      "description": "Background information about quiz",
      "required": true,
      "type": "string"
    },
    {
      "name": "numberOfQuestions",
      "description": "number of questions to generate",
      "required": true,
      "type": "integer"
    },
    {
      "name": "typeOfQuestions",
      "description": "Array of question types to include in the quiz. You can specify multiple types (e.g., ['MultipleChoice', 'TrueFalse']).",
      "required": true,
      "type": "array",
      "items": {
        "type": "string",
        "enum": [ "MultipleChoice", "TrueFalse" ]
      }
    }
  ]
}