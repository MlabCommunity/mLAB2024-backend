{
  "schema": 1,
  "type": "completion",
  "description": "Generate quiz based on the given user topic, number of questions, number of answers and correct answer. Respond only with JSON.",
  "execution_settings": {
    "default": {
      "temperature": 0,
      "response_format": {
        "type": "json_object"
      }
    }
  },
  "input_variables": [
    {
      "name": "topic",
      "description": "Background information about quiz",
      "required": true
    },
    {
      "name": "numberOfQuestions",
      "description": "number of questions to generate",
      "required": true
    },
    {
      "name": "numberOfAnswers",
      "description": "number of answers to generate per question",
      "required": true
    },
    {
      "name": "numberOfCorrectAnswers",
      "description": "number of correct answers to generate per question",
      "required": true
    }
  ]
}