{
  "schema": 1,
  "type": "completion",
  "description": "Generate quiz based on the given user topic, number of questions, and type of answers. Respond only with JSON.",
  "execution_settings": {
    "default": {
      "temperature": 0.8,
      "response_format": {
        "type": "json_object"
      }
    }
  },
  "input_variables": [
    {
      "name": "content",
      "description": "Information from attachments provided by user",
      "required": true
    },
    {
      "name": "numberOfQuestions",
      "description": "Number of questions to generate",
      "required": true
    },
    {
      "name": "typeOfQuestions",
      "description": "Type of questions to generate.",
      "required": true
    }
  ]
}